services:
  openmaic:
    build:
      context: .
      dockerfile: Dockerfile.production
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
    restart: unless-stopped
